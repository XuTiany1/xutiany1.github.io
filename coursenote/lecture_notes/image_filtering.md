

# Image filtering

## Image noise
There is a lot of noise, even if we take the same photo twice. 


## Edge
An example of an edge(suppose no noise) there is a good graph that shows this from the slide. 

## Guassian
the first fraction is just a normalization factor

mew is mean

If we have I.I.D. drawn samples, and if we keep drawing (law of large number), then it converges to a gaussian.


Soem properties:
- Integrates to 1
- Can use it for smoothing weights
    - As we use a bigger scale sigma, we can reduce the noise and average together more pixels (blurring the signal more)




## Cross Correlation

Cross Correlation of two signals.

- This does not have the commutative property while convolution does. 


## Convolution

Cross correlation you have f(u-x) in the sum while in convolution, we have f(x-u) in the sum. 

f(x) is usually called FILTER OR KERNEL

I(x) is the SOURCE





























































